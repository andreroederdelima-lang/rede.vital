name: Diag Reusable

on:
  push:
    branches: [claude/fix-ci-registry]

jobs:
  test:
    uses: andreroederdelima-lang/actions-templates/.github/workflows/node-ci.yml@main
    with:
      node-version: '22'
      package-manager: 'auto'
